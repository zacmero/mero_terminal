[mgr]
prepend_keymap = [
    { on = "<C-c>", run = "quit", desc = "Quit and keep the current directory" },
    { on = "I", run = "plugin toggle", desc = "Toggle file-size linemode" },
    { on = [ "g", "z" ], run = "plugin zoxide", desc = "Jump to a directory via zoxide" },
]
