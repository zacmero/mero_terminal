Follow the repo mission in GEMINI.md and AGENTS.md: portability, unattended installs, centralized configs, and README synchronization. Shell changes should remain idempotent and backup existing files before relinking. New subprojects should be isolated in their own folders. For Python code in gh-home/, prefer dataclasses, explicit adapter boundaries, stdlib-first dependencies, and graceful CLI error handling instead of raw tracebacks.